# Microsoft OAuth
NEXT_PUBLIC_MSAL_CLIENT_ID=
NEXT_PUBLIC_MSAL_TENANT_ID=common
NEXT_PUBLIC_MSAL_REDIRECT_URI=http://localhost:4000/availability/connect

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Upstash Redis (add via Vercel Marketplace → Storage → Upstash Redis)
# Vercel auto-populates these when you connect the integration
KV_REST_API_URL=
KV_REST_API_TOKEN=
